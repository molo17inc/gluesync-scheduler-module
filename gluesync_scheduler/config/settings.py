# This file is kept for backward compatibility but is no longer used.
# All settings are now accessed directly via os.getenv() calls throughout the codebase.

# Empty stub to prevent import errors
class Settings:
    """Minimal settings class for backward compatibility"""

    def __init__(self):
        pass

settings = Settings()
